const profile = require('./profile');

const createPro = require('./createPro');

const post = require('./post');

const createPos = require('./createPos');

module.exports = { profile, createPro, post, createPos }